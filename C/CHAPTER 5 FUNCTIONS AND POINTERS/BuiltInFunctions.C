#include<stdio.h>
#include<math.h>
int  main(){
    // int a;
    // printf("Enter the base :");
    // scanf("%d",&a);
    // int b;
    // printf("Enter the Power : ");
    // scanf("%d",&b); 
    // int p = pow(a,b);
    // printf("%d raised to the %d is %d ",a,b,p);
    int n;
    printf("Enter the number :");
    scanf("%d",&n);

    int root = sqrt(n);
    printf("Vale is  %d ",root);
    
    
    return 0;
} 